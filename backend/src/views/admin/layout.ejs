<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' — ' : '' %>Admin — Alex Palaghia</title>
  <link rel="stylesheet" href="/admin/static/admin.css">
</head>
<body>
  <div class="admin-layout">
    <nav class="sidebar">
      <div class="sidebar__brand">
        <strong>Alex Palaghia</strong>
        <span>Admin</span>
      </div>
      <ul class="sidebar__nav">
        <li><a href="/admin" class="sidebar__link<%= typeof activePage !== 'undefined' && activePage === 'dashboard' ? ' sidebar__link--active' : '' %>">Dashboard</a></li>
        <li><a href="/admin/films" class="sidebar__link<%= typeof activePage !== 'undefined' && activePage === 'films' ? ' sidebar__link--active' : '' %>">Films</a></li>
        <li><a href="/admin/commercials" class="sidebar__link<%= typeof activePage !== 'undefined' && activePage === 'commercials' ? ' sidebar__link--active' : '' %>">Commercials</a></li>
        <li><a href="/admin/gallery" class="sidebar__link<%= typeof activePage !== 'undefined' && activePage === 'gallery' ? ' sidebar__link--active' : '' %>">Gallery</a></li>
        <li><a href="/admin/settings" class="sidebar__link<%= typeof activePage !== 'undefined' && activePage === 'settings' ? ' sidebar__link--active' : '' %>">Settings</a></li>
      </ul>
      <form method="POST" action="/admin/logout" class="sidebar__logout">
        <button type="submit" class="btn btn--ghost">Logout</button>
      </form>
    </nav>

    <main class="main-content">
      <% if (typeof flash !== 'undefined' && flash) { %>
        <div class="alert alert--success"><%= flash %></div>
      <% } %>
      <%- body %>
    </main>
  </div>
</body>
</html>
